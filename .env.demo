# Demo Configuration - No Real API Keys
# This file is for public demo deployment

# Demo mode - will use mock responses
DEMO_MODE=true
NODE_ENV=production

# Public config (safe to expose)
NEXT_PUBLIC_APP_NAME=ZigsAI ULTRA Agent Studio
NEXT_PUBLIC_VERSION=1.0.0
NEXT_PUBLIC_DEMO=true

# API endpoints (no keys)
NEXT_PUBLIC_API_URL=/api
PORT=3000

# Feature flags
ENABLE_MOCK_DATA=true
ENABLE_DEMO_MODE=true
DISABLE_EXTERNAL_APIS=false

# Note: Real API keys should be set as secrets in deployment platform
# Never commit real API keys to repository!
